---
import Layout from '../layouts/Layout.astro';
import Donate from '../components/Donate.astro';
import donate from '../content/donate';
---

<Layout title="Recommended Donations for the Perno Family">
	<main class="text-center">
		<h1>Make the World a Better Place</h1>
		<p>We treasure the excitement around the upcoming little one, but we ask that the celebration <em>not</em> include purchasing new things. If you have the money to spend, we ask that you donate it in the baby's honor instead. We have provided a vetted list of worthwhile charities.</p>
		<ul class="list-unstyled">
			{donate.map(item => (
				<Donate
					title={item.title}
					description={item.description}
					link={item.link}
				/>
			))}
		</ul>
	</main>
</Layout>

<style>
	h1 {
		margin: 2rem 0;
	}

	main {
		margin: auto;
		padding: 1em;
		max-width: 60ch;
	}
	.list-unstyled {
		padding-left: 0;
    list-style: none;
	}
	.text-center {
		text-align: center;
	}
	p {
		margin-bottom: 3rem;
	}
</style>
